{
  "name": "gravity",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w @gravity/api\" \"npm run dev -w @gravity/web\"",
    "build": "npm run build -w @gravity/shared && npm run build -w @gravity/api && npm run build -w @gravity/web",
    "dev:web": "npm run dev -w @gravity/web",
    "dev:api": "npm run dev -w @gravity/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
